{
  "worldSettings": {
    // Biome size settings per noise setting, set by default to no changes. Gets automatically filled on world creation. Restart after changing values.Note that the biome size changes work indirectly by modifying temperature and vegetation zone sizes and you cannot change the size of a specific biome
    "minecraft:amplified": {
      // Set the multiplicative modifier for biome size. 1.0 = Default size, 2.0 = bigger, 0.5 = smaller.
      "size_multiplier": 1.0
    },
    "minecraft:caves": {
      // Set the multiplicative modifier for biome size. 1.0 = Default size, 2.0 = bigger, 0.5 = smaller.
      "size_multiplier": 1.0
    },
    "minecraft:floating_islands": {
      // Set the multiplicative modifier for biome size. 1.0 = Default size, 2.0 = bigger, 0.5 = smaller.
      "size_multiplier": 1.0
    },
    "minecraft:large_biomes": {
      // Set the multiplicative modifier for biome size. 1.0 = Default size, 2.0 = bigger, 0.5 = smaller.
      "size_multiplier": 1.0
    },
    "minecraft:overworld": {
      // Set the multiplicative modifier for biome size. 1.0 = Default size, 2.0 = bigger, 0.5 = smaller.
      "size_multiplier": 0.2
    },
    "minecraft:end": {
      // Set the horizontal size offset, this dimension only has limited size changing support. You can only step sizes in whole numbers, not percentages. 0 = Default size, 1 = bigger, -1 = smaller. Range[-1,2]
      "horizontal_size": 0
    },
    "minecraft:nether": {
      // Set the horizontal size offset, this dimension only has limited size changing support. You can only step sizes in whole numbers, not percentages. 0 = Default size, 1 = bigger, -1 = smaller. Range[-1,2]
      "horizontal_size": 0
    }
  }
}
